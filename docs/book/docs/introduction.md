# Introduction

`archr` is a headless Rust engine for ArchiMate 3.2.

## What it does

- **Validate** models against the derivability ruleset
- **Generate** Open Exchange XML from YAML
- **Parse** XML back to YAML
- **Diff** two models

[Install →](/installation)